version: '3.7'

networks:
  backend:
    name: backend
    driver: bridge

services:
  registry-frontend:
    build:
        context: "."
    ports:
      - 4200:4200
    environment:
      - PROJECT=dev
      - CONFIGURATION=production
      - PROXY_CONFIG=src/proxy.conf.json
    networks:
      - backend
